## ADDED Requirements

### Requirement: Overview metric visibility

The overview page SHALL display all provider metric progress lines needed for the summary view.

#### Scenario: Provider has multiple metric scopes

- **WHEN** a provider exposes metric lines outside the `overview` scope
- **THEN** the overview page includes those metric lines in the progress list
